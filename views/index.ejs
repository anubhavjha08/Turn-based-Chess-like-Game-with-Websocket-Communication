<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chess-like</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .chessboard {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            grid-template-rows: repeat(5, 1fr);
            gap: 0;
            border: 5px solid rgb(2, 228, 209);
        }

        .square {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .light {
            background-color: #f3f4f6;
        }

        .dark {
            background-color: #030302;
        }

        .piece {
            font-size: 2rem;
            cursor: grab;
            transition: transform 0.2s ease;
        }

        .piece.player1 {
            color: #e11d48;
        }

        .piece.player2 {
            color: #3b82f6;
        }

        .piece.player1:active,
        .piece.player2:active {
            cursor: grabbing;
            transform: scale(1.1);
        }

        .rotate-board {
            transform: rotate(180deg);
        }

        .rotate-piece {
            transform: rotate(180deg);
        }
    </style>
</head>

<body class="bg-zinc-900 flex items-center justify-center h-screen">
    
    <div class="flex items-center space-x-6">
        <div class="chessboard w-96 h-96 bg-zinc-700"></div>
        <div class="chat-container w-96 h-96 bg-zinc-800 text-zinc-100 rounded-md flex flex-col">
            <div id="messages" class="messages flex-grow overflow-y-auto p-4 space-y-2"></div>
            <form id="message-form" class="message-form flex p-4 border-t border-zinc-700">
                <input type="text" id="message-input"
                    class="message-input flex-grow p-2 rounded-md bg-zinc-700 text-zinc-100 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-teal-400"
                    placeholder="Type your message..." required />
                <button type="submit"
                    class="message-send ml-2 px-4 py-2 rounded-md bg-teal-500 text-zinc-100 hover:bg-teal-600">Send</button>
            </form>
        </div>
    </div>

    <script src="https://cdn.socket.io/4.7.5/socket.io.min.js"
        integrity="sha384-2huaZvOR9iDzHqslqwpR87isEmrfxqyWOF7hr7BY6KG0+hVKLoEXMPUJw3ynWuhO"
        crossorigin="anonymous"></script>
    <script src="/js/clientLogic.js"></script>
</body>

</html>